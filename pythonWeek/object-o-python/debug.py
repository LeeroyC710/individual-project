import pdb

pdb.set_trace()
a = "aaa"
b = "bbb"
c = "ccc"

final = a + b + c
print(final)

#to run theough the code by each line please press n and enter to run through it. 
# q to quit the debugger
# p plus the variable for example (p c) after running though each line
